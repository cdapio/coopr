{
  "name": "base",
  "description": "Base services, such as host management and firewall",
  "dependson": [
  ],
  "provisioner": {
    "actions": {
      "install": {
        "type": "chef-solo",
        "script": "recipe[loom_base::default]"
      },
      "configure": {
        "type": "chef-solo",
        "script": "recipe[loom_base::default]"
      }
    }
  }
}

{
    "name": "overrides-partial",
    "description": "Tries to override immutable config property ('sensu_wrapper')",
    "immutable": false,
    "defaults": {
        "config": {
            "sensu_wrapper": {
                "additional_client_attributes": {
                    "mail_to": "%cluster.owner%@cask.co"
                },
                "endpoint": "sensu-internal-server.cask.co"
            }
        }
    }
}

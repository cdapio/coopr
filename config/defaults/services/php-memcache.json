{
  "name": "php-memcache",
  "description": "PHP memcached support",
  "dependencies": {
    "conflicts": [],
    "install": {
      "requires": [
        "base",
        "php"
      ],
      "uses": []
    },
    "provides": [],
    "runtime": {
      "requires": [
        "php",
        "memcached"
      ],
      "uses": []
    }
  },
  "provisioner": {
    "actions": {
      "install": {
        "type":"chef-solo",
        "fields": {
          "run_list": "recipe[php::module_memcache]"
        }
      },
      "configure": {
        "type": "chef-solo",
        "fields": {
          "run_list": "recipe[php::default]"
        }
      }
    }
  }
}

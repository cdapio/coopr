{
  "name": "php",
  "description": "PHP language support",
  "dependson": [
    "apache-httpd"
  ],
  "provisioner": {
    "actions": {
      "install": {
        "type":"chef",
        "script": "recipe[php::default]"
      },
      "configure": {
        "type": "chef",
        "script": "recipe[php::default]"
      }
    }
  }
}

#!/usr/bin/env ruby

unless $:.include?(File.dirname(__FILE__) + '/../lib')
  $: << File.dirname(__FILE__) + '/../lib'
end

require 'provisioner/api'

options = Loom::CLI.read
Loom::Provisioner.run(options)
